<head>
  <title>${{title}}</title>
</head>
<div class="card-container">
  <h1 class="card-title">Iniciar sesión</h1>
  <form action="/users/login" method="POST">
    <div class="form-group">
      <label for="mentoringClass" class="form-label">Correo electrónico</label>
      <input type="email" name="userEmail" class="form-input" placeholder="ejemplo@email.com">
    </div>
    <div class="form-group">
      <label for="mentoringTopic" class="form-label">Contraseña</label>
      <input type="password" name="userPassword" class="form-input" placeholder="contraseña">
    </div>
    
    <button class="btn btn-primary">Iniciar sesión
    </button>
  </form>
</div>
